{
  "name": "projectpilot-root",
  "version": "1.0.0",
  "description": "Root package for full-stack deployment",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  }
}
